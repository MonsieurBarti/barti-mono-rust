# Land the glossaries and context map

Type: task
Label: wayfinder:task
Blocked by:
Status: resolved

## Question

Copy the fenced glossary files from [the locked spec](../../hive-docs/spec.md) into the repo.

Write repo-root `CONTEXT-MAP.md` and `crates/cells/freight/loads/CONTEXT.md`. Drop Load, Quote, Stop, Consignee, Shipper, and Carrier from root `### Freight`. Update `docs/agents/domain.md` so it names the map and the `loads` glossary.

Match the fenced copies. Do not add handbook pages, OpenAPI, or CI.

## Answer

Copied the fenced glossary files from the locked spec. Root `### Freight` dropped the six loads terms. `docs/agents/domain.md` names `CONTEXT-MAP.md` and the `loads` glossary.
