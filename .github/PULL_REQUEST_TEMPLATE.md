## Ticket

Link the ticket path.

## Answer

One gist sentence.

## Gate

Name each command and its result.

## Attack campaign

Paste the local-attack plan. Every case has a status. End with `local-attack: green` or `local-attack: red`.

When the skill skipped, write `local-attack: green (no surface)`.

## Warns

List each `review-change` warn you kept, and why. Write `None.` when there are none.
