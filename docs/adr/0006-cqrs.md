# CQRS

One use-case struct implements the API port so presentation never sees a bus. Write SPI is `get_by_id` / `save` with no unit-of-work because two saves are two transactions. The use-case publishes through the events SPI after commit so handlers may lag.

Chapter: [5. CQRS](../architecture.md#5-cqrs)

Grill: [How does CQRS work inside a Rust cell?](../../.scratch/rust-hive/issues/14-cqrs-inside-a-cell.md)
